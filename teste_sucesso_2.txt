In v[3].
Rac r.
In x.
x => 1.
v[0] => 10.
v[x] => 20.
v[2] => 30.

CRIAR Rac calcular(In val, Rac mult) {
    VOLTA val * mult.
}

r => calcular(v[0], 2). :3 Type promotion: passing integer 2 as rational parameter, and calculating rational * integer
x => 0.

Enq (x ?< 3) {
    v[0] => v[0] + 1. :3 Use constant index 0 because the SaM generator only supports constant indices
    x => x + 1.
}
